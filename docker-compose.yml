services:
  db:
    container_name: db-game
    image: postgres:18.2
    env_file:
      - ./db/.env
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql

volumes:
  db_data:
    driver: local
